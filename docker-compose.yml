services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-VIImpactDb}
      POSTGRES_USER: ${POSTGRES_USER:-viimpact}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Defina POSTGRES_PASSWORD no arquivo .env}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          pg_isready
          -U "$${POSTGRES_USER}"
          -d "$${POSTGRES_DB}"
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 10

  api:
    image: viimpact-api:local
    restart: unless-stopped
    build:
      context: .
      dockerfile: VIImpact.API/Dockerfile
      args:
        BUILD_CONFIGURATION: Release
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: >-
        Host=postgres;
        Port=5432;
        Database=${POSTGRES_DB:-VIImpactDb};
        Username=${POSTGRES_USER:-viimpact};
        Password=${POSTGRES_PASSWORD};
        SSL Mode=Disable
      Cors__AllowedOrigins: ${CORS_ALLOWED_ORIGINS:-http://localhost:5173}
      TwelveData__ApiKey: ${TWELVE_DATA_API_KEY:?Defina TWELVE_DATA_API_KEY no arquivo .env}
      TwelveData__BaseUrl: ${TWELVE_DATA_BASE_URL:-https://api.twelvedata.com}
      StockCollection__Enabled: ${STOCK_COLLECTION_ENABLED:-true}
      StockCollection__Symbol: ${STOCK_SYMBOL:-TTWO}
      StockCollection__IntervalMinutes: ${STOCK_INTERVAL_MINUTES:-5}
    ports:
      - "${API_PORT:-5170}:8080"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - --silent
        - --show-error
        - http://localhost:8080/health/ready
      interval: 15s
      timeout: 5s
      start_period: 30s
      retries: 8

  web:
    image: viimpact-web:local
    restart: unless-stopped
    build:
      context: .
      dockerfile: VIImpact.Web/Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    ports:
      - "${WEB_PORT:-5173}:80"
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres-data:
